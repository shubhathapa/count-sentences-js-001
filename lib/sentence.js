'use strict';

const countSentences = function(sentences){
  let count =0;

for(let char of text){
  if(char === '.' || char == '!' || char === '?'){
    count ++;
  }
}
return count;
  
};

module.exports = countSentences;
